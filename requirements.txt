# Install all dependencies with:
#     pip install -r requirements.txt

Django>=4.2,<5.1
Pillow>=10.0
whitenoise>=6.6        # serves static files in production (Railway, PythonAnywhere)
gunicorn>=21.0         # production web server (Railway uses this)
openpyxl>=3.1          # CSV/Excel export
